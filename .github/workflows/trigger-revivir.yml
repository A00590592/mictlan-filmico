name: 🔔 Activar Revivir

on:
  workflow_dispatch:      # Permite ejecutarlo manualmente
  repository_dispatch:
    types: [revivir_trigger]

permissions:
  contents: read

jobs:
  mqtt:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Enviar señal al altar (MQTT)
        run: |
          echo "🪔 Enviando señal al altar físico..."
          mosquitto_pub -h "broker.hivemq.com" -p 1883 -t "mictlan/altar" -m "revivir" -q 1
          echo "✅ Señal enviada con éxito."
